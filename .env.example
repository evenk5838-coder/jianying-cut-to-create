# Optional: an HTTPS URL for a soundtrack whose website playback is authorized.
# The URL will be public in the deployed browser; do not put secrets here.
VITE_AUDIO_URL=
# GitHub Project Pages example: /your-repository-name/
BASE_PATH=./

# Optional public MP4 directory, pinned to an immutable release/commit.
VITE_VIDEO_BASE_URL=
